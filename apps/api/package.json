{
  "name": "@grocery/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "migrate:local": "wrangler d1 migrations apply grocery-list --local",
    "migrate:remote": "wrangler d1 migrations apply grocery-list --remote"
  },
  "dependencies": {
    "@grocery/shared": "*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260907.1",
    "vitest": "^5.0.0",
    "wrangler": "^4.129.0"
  }
}
