{
  "name": "grocery-list",
  "version": "0.1.0",
  "private": true,
  "description": "A shared grocery list for two people, that actually syncs.",
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev:api": "npm run dev -w @grocery/api",
    "dev:web": "npm run dev -w @grocery/web",
    "typecheck": "tsc --build && tsc -p apps/api/tsconfig.test.json && tsc -p apps/web/tsconfig.test.json",
    "test": "npm run test --workspaces --if-present",
    "build": "npm run build -w @grocery/shared && npm run build -w @grocery/web",
    "preview": "npm run build && npm run dev -w @grocery/api",
    "deploy": "npm run build && npm run deploy -w @grocery/api",
    "icons": "python3 scripts/generate-icons.py"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^22.0.0"
  },
  "engines": {
    "node": ">=22"
  }
}
